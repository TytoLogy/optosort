Code to export/import opto data to spike sorting programs. 

Initial target is for plexon Offline Sorter (OFS) version 3.x

optoproc (within the OptoAnalysis repository) will already export to wav_clus)